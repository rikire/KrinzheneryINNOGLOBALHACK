from app.storage.crud.users import read_all_users

async def fetch_search(querry:str):
    # parts = querry.split()
    # result = []
    # users = await read_all_users()
    # users_marks = []
    # for user in users:
    #     mark = 0
    #     for part in parts:
    #         if part in user.competencies:
    #             mark += 1000
    #         if part in user.
    return None
    